{
  "name": "@jarvis-tax/agent",
  "version": "1.0.0",
  "description": "Jarvis autonomous agent runtime. ReAct execution loop with tool registry, approval gates, and memory.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx src/index.ts",
    "agent": "tsx src/cli.ts",
    "test": "tsx src/test-agent.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "openai": "^4.70.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
